@{
    // i will remove it and add _Dashboard Layout to make all admin views start with _Dashboard Layout 
    // and after add these , i will be not need to add these ( Layout = "~/Views/Shared/_Dashboard.cshtml";) in all admin views
    // Layout = "_Layout";
    Layout = "_Dashboard";
}
